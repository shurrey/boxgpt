openapi: '3.0.2'
info:
  title: Box GPT
  version: '1.0'
servers:
  - url: https://boxgpt.herokuapp.com
paths:
  /folders:
    get:
      responses:
        '200':
          description: OK
  /files:
    get:
      responses:
        '200':
          description: OK